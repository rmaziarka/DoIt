This module contains functions that can be used during building. They are not available during deployment.
Each function in `BuildPackage` directory creates a package in `bin` directory (by default), 
that can be used by corresponding deploy function.
